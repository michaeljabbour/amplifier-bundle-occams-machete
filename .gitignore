# Amplifier dev-session scaffolding (not part of the bundle)
AGENTS.md
# Coordination files moved under the hidden per-repo directory
# (amplifier-bundle-memory hooks-project-context 1.2.0); the bare name is kept
# so an older clone that still has them at the root stays ignored too.
.amplifier/project-context/
project-context/

# Recipe run state
**/recipe-sessions/

# Python / OS noise
__pycache__/
*.pyc
.DS_Store

# Per-module lockfiles from local test runs (modules take amplifier-core as a peer dep)
**/uv.lock

# Tooling caches (ruff, pytest)
.ruff_cache/
**/.ruff_cache/
.pytest_cache/
**/.pytest_cache/

# Local DTU / amplifier state (reality-check profiles, etc.)
.amplifier/

# Personal working notes (kept local, not shipped)
evaluation/MJ-FEEDBACK-AND-PROMPTS.md
.mj-rename-verification.png
.mj-rename-verification.gif
.mj-rename-verification.mp4
.mj-rename-verification.html
